"""
Versioned prompt assets for the extraction pipeline.

Prompts live as plain text files under sub-directories named after each prompt
(e.g. `deal_analysis/v1.txt`). Loading is done via `prompts.registry.load_prompt`.
"""
